<%= form_for(@trait) do |f| %>
<%= render 'shared/error_messages', object: f.object %>

    <%= f.label :trait_name %>
    <%= f.text_field :trait_name %>

    <%= f.submit class: 'btn btn-large' %>

<% end %>
